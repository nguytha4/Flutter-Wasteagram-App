class Post {
  DateTime date;
  String dateTime;
  String imageURL;
  int quantity;
  double latitude;
  double longitude;

  Post({this.date, this.dateTime, this.imageURL, this.quantity, this.latitude, this.longitude});
}